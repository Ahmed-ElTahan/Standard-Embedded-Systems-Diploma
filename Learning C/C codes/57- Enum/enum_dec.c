// This code is created by Ahmed A. Eltahan

/* 
	----------------------------- Program Description -----------------------------------------
	This program is used to show how to declare user-defined emum data types

*/

// Libraries
#include<stdio.h> // Library to include c functions for the compiler.

// Function prototype
// Declared Structures
// Declared Functions
// Global variables (Declaration and Inintialization)
// Enum
	// enum declaration
	enum suit {club, diamonds, hearts, spades}; // enum name is "suit" , suit values are {0, 1, 2, 3}, suit is just a reference not a variable
	// other way to declare
		//enum suit {club, diamonds, hearts, spades} state; // state is the enum variable of enum suit

/*
// Changing default values of enum
enum suit {
    club = 0,
    diamonds = 10,
    hearts = 20,
    spades = 3
};
*/
void main(void)
{	
	// Declaration and Inintialization
	enum suit state; // enum type variable declaration.
	
	// Write your instructions here.
	// infinite loop  while(1){}
	// Output
	
}